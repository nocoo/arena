{
  "name": "@arena/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "arena": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --max-warnings=0",
    "test": "bun run build && bun --bun vitest run",
    "test:unit": "bun --bun vitest run tests/utils.test.ts tests/push.unit.test.ts tests/pop-status.unit.test.ts",
    "test:integration": "bun run build && bun --bun vitest run tests/push.test.ts tests/pop-status.test.ts",
    "test:coverage": "vitest run --coverage tests/utils.test.ts tests/push.unit.test.ts tests/pop-status.unit.test.ts"
  },
  "dependencies": {
    "@arena/core": "workspace:*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
